name: mobile

# The Flutter app, including the instrument side; its simulator tests spawn hardware/sim.
on:
  push:
    paths:
      - 'mobile/**'
      - 'hardware/sim/**'
      - 'hardware/data/**'
      - '.github/workflows/mobile.yml'
  pull_request:
    paths:
      - 'mobile/**'
      - 'hardware/sim/**'
      - 'hardware/data/**'
      - '.github/workflows/mobile.yml'

jobs:
  app:
    name: flutter app
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'          # the simulator tests spawn hardware/sim/fake_board.py
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.47.5
          cache: true
      - name: pub get
        working-directory: mobile
        run: flutter pub get
      - name: analyze
        working-directory: mobile
        run: flutter analyze
      - name: test
        working-directory: mobile
        run: flutter test
      - name: build apk
        working-directory: mobile
        run: flutter build apk --debug
      - uses: actions/upload-artifact@v4
        with:
          name: peel-mobile-debug-apk
          path: mobile/build/app/outputs/flutter-apk/app-debug.apk
